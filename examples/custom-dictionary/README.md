# 自訂詞典

這個範例示範 `custom_converter()` 與 `converter_factory()`。

## 重點

- `custom_converter()` 適合只套用自己的詞典。
- `converter_factory()` 可以把內建詞庫與自訂詞庫串接。
- 較長詞條會優先匹配，例如「用户界面」優先於「用户」。

## 執行

```bash
python examples/custom-dictionary/example.py
```
