# 基本簡繁轉換

這個範例示範如何使用 `opencc_py.converter("cn", "tw2")` 將中國大陸簡體中文轉成臺灣繁體中文。

## 重點

- `converter()` 回傳可呼叫物件，可直接像函式一樣使用。
- `cn -> tw2` 適合臺灣使用者介面與一般內容。

## 執行

```bash
python examples/basic-conversion/example.py
```
